
.row-fluid
  .hero-unit
    %h3 Scoring by photos' number and location

.row-fluid
  - @sorted_users.each do |user|
    .span4
      %h3
        %a{ :href => "/user/#{user.id}" } #{user.username}
        - if user.score == 1
          %small 1 point
        - else
          %small #{user.score} points
      %img{ :src => user.profile_picture }
